# 生产化改造与验收记录

验证日期：2026-09-07（America/Tijuana）。工作目录：`D:\workspace\As-a`。

**状态：本地改造及验证完成，可进入真实预发布环境验收；尚未授权或执行正式上线。** 本次没有接入真实 Supabase、SMTP 或 Tap 商户，也没有修改远端数据库、提交、推送或部署。

## 已落实

- 支付：官方签名协议、权威交易回查、金额/币种/环境/订单绑定校验、幂等结算、原子交付确认。未开始支付的订单可安全取消；支付启动与取消互斥。
- 提现：原子预留余额，人工核对转账后登记凭据；移除假开户及未经实现的自动转账。
- 权限：完整数据库初始结构、RLS、敏感字段保护、公开资料投影、账户停用、服务审核、管理员事务与持久审计。
- 账户：邮箱确认、安全回跳、密码重置、完整导出、删除请求及资金/订单阻断条件。
- 前端：全局通知、错误反馈、中英文切换、手机布局、支付回跳竞态、取消确认，清除虚构运营数字、资质、套餐和占位联系方式。
- AI：登录校验、请求限额、数据库共享限流、超时、错误脱敏；客户端对话长度与接口限额一致。
- 运维：环境校验、独立 smoke 构建、拒绝错误产物的启动入口、Docker、健康探针、响应安全头、CI 与依赖更新配置。
- 依赖：Next.js 16.3.4、React 19.2.8、Supabase 客户端更新及传递依赖修复。版本选择核对了 [Next.js 官方安全公告](https://nextjs.org/blog/august-2026-security-release)。

## 验证证据

| 项目 | 结果 |
| --- | --- |
| 最终单元 / API / UI 回归 | 17 个文件、157 项测试全部通过 |
| Chromium 浏览器验收 | 20 项全部通过，包括受保护路由、密码恢复、404、响应头、中英文手机与桌面渲染 |
| PostgreSQL 实际执行 | 5 份迁移、47 条权限断言，以及支付和管理员 SQL 回归全部通过 |
| TypeScript / ESLint | 通过，无 lint 警告 |
| 生产模式构建 | Next.js 优化构建通过，输出到独立 `.next-smoke`，使用隔离假配置 |
| 依赖审计 | `npm audit`：0 项漏洞 |
| 启动保护 | 缺密钥、产物错配、smoke 误启动会失败；staging/test 与 production/live 模式测试通过 |
| 差异检查 | `git diff --check` 通过 |

完整 `npm run verify` 已通过；其后补充的 staging/启动打包用例经过最终 157 项测试、Lint 与类型检查。应用页面未在浏览器验收后发生变更。使用 Node 24.19.0 验证；本机默认 Node 26 的浏览器包解压出现卡住，改用 Node 24 后安装与验证正常，未更改系统 Node 配置。

PGlite 运行真实 PostgreSQL 迁移和事务，但使用最小 Auth/Storage 模拟及单连接。它不能代替真实 Supabase 网关、邮件、Realtime、Storage MIME 检查、Tap 支付或多连接并发验收。Docker 不可用，因此未构建运行镜像；启动文件打包布局已通过隔离子进程测试。

## 正式上线前仍需完成

1. 确认项目是否已有真实用户/订单、目标部署平台和独立预发布数据库。旧库必须先盘点 schema/RLS、备份与恢复演练，不能盲套初始迁移。
2. 在预发布环境配置 Supabase、SMTP、真实回调域名，用买家/服务商/管理员/陌生用户完成全链路验收。
3. 完成 Tap 商户/KYC、sandbox 支付与回调、幂等及并发测试，确认人工提现、退款/争议与日常对账负责人，再开放正式付款。
4. 补齐真实运营主体、联系方式、正式隐私/服务条款、数据保留政策、监控接收者及恢复流程。法务页目前明确标为待审核草案。

`TAP_PAYMENTS_ENABLED`、`AI_CHAT_ENABLED`、分析统计默认关闭。已开始付款但结果未知的订单须核对商户记录，不能直接清除 `checkout_started_at`。申请删除账户不等于立即物理抹除全部数据。

## 交接与运行状态

- Git：仍在 `main`，改动留在工作区，未暂存、提交、推送。
- 测试服务器：3107 端口已释放；原有 3000 端口进程未改动。
- 产物：`node_modules`、构建缓存、`playwright-report` 与 `test-results` 截图保留供复核，均被 Git 忽略。旧 pnpm 锁文件已移除，原版本可从 Git 恢复，npm 为唯一安装来源。
- 操作指南：[生产部署与回滚](production.md)、[数据库安装/旧库采用](../supabase/README.md)、[项目说明](../README.md)。
