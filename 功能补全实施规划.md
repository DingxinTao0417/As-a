# As’a 功能补全实施规划

版本：1.0 · 日期：2026-09-08 · 审查基线：`c4403a1`

本文用于把现有项目补全为能够稳定完成真实业务的服务平台。全部任务初始状态为 **Backlog**；本文中的目标、建议数据表和验收场景均是待实施内容，不代表当前已经具备。后续按任务编号执行，并在完成后补充验证证据。

## 1. 当前基础与完成目标

项目面向沙特市场，支持阿拉伯语和英语，包含需求方、服务商、管理员三个主要角色。现有技术栈为 Next.js 16、React 19、TypeScript、Supabase Auth / PostgreSQL / Storage / Realtime、Tap Payment，以及 DeepSeek / OpenAI 客服接口。

已经有真实代码链路：账户注册登录、服务发布与浏览、聊天与报价、订单付款入口、交付验收、评价、服务商工作台和后台管理。缺口主要集中在资金一致性、权限可验证性、业务异常恢复、静态功能落地和运营收尾。

完成目标是让下面的流程及其失败、重试、取消和恢复路径都有明确行为：

> 注册与入驻 → 服务审核与发布 → 搜索与收藏 → 沟通与报价 → 下单付款 → 交付与验收 → 评价与结算 → 提现与售后。

同时完成账户导出与注销、客服与人工支持、套餐订阅及其权益、双语与移动端体验、管理员操作和运行维护。

### 1.1 已执行的基线检查

以下结果来自本次会话前一轮审查，后续实施时应重新验证：

| 项目 | 基线结果 | 说明 |
| --- | --- | --- |
| 单元测试 | 8/8 通过 | 主要覆盖金额计算、结果封装和错误类型，不能证明交易流程正确 |
| TypeScript | 通过 | `npx tsc --noEmit --incremental false` |
| Lint | 0 错误、3 警告 | 仍有未使用变量警告 |
| 生产构建 | 通过 | 使用本地占位 Supabase 配置，无真实业务联调 |
| Playwright | 4/5 通过 | 登录页测试的 `getByRole("heading")` 多匹配；表单本身正常渲染 |
| 页面检查 | 首页桌面/手机、语言切换与刷新保留正常 | 未覆盖登录后的完整业务界面 |
| 真实外部服务 | 未验收 | 未执行真实 Supabase 写入、付款、提现或模型调用 |

仓库仅有一份依赖已有表的增量数据库迁移，未提供完整建表、RLS、触发器、RPC 和 Storage 初始化。这里的结论是“数据库状态未被验证”，不能解释成“线上没有权限策略”。

复核校正：`app/my-services/page.tsx` 的**创建**流程写入 `is_active: true`；当前普通编辑流程没有写入该字段。需要解决的是新建即上架、审核与发布状态混用，以及数据库是否允许绕过审核的问题，不应将“编辑必然重新上架”当成已证实缺陷。

### 1.2 使用约定

1. 每次选择一个任务编号，先查看依赖和当前代码，再实施与验证；不要根据文档直接执行生产 SQL。
2. 文件路径均相对于仓库根目录；标记为“建议新增”的文件、表和状态需要在实施时与现有数据库核对。
3. P0 表示权限、资金或可靠交易的阻塞；P1 表示主要业务闭环；P2 表示完整产品体验。优先级不改变最终交付范围。
4. 推荐顺序是 BASE → ACC/CAT → MSG/ORD → PAY → OPS/DATA → AI/PLAN → UX/REL。不存在依赖冲突的任务可以并行。
5. 文档不预先授权生产迁移、对外发送、付费、部署或远端 Git 操作。开发使用隔离测试环境；真实联调和上线另按明确授权执行。

## 2. 实施前需要记录的业务决策

这些决策只阻塞依赖它们的任务。可以先完成数据库盘点、测试设施、错误反馈等独立工作。决策记录至少包含：最终选择、负责人、日期、规则版本、影响任务和已有数据处理方式。

| 编号 | 需要确定的内容 | 建议工作基线，尚非最终业务规则 | 影响任务 |
| --- | --- | --- | --- |
| D-01 | 报价是否含税、税的计算基础、佣金承担方、折扣与退款分摊 | 保留 SAR；把服务基价、税额、客户实付、平台收入和服务商净额分别存储。当前代码的 15% 不能代替业务及税务确认 | ORD-02、PAY-02、OPS-01、PLAN-01 |
| D-02 | Tap 实际开通的收款、服务商账户、分账、转账、退款、订阅能力 | 先核对商户合同与测试权限；服务商提现统一走申请、审核、执行、对账。不得仅凭当前 wrapper 假定接口可用或已经实现资金托管 | PAY-01 至 PAY-05、PLAN-02 |
| D-03 | 验收期限、修改次数、逾期、拒收、取消、部分退款、争议与已提现退款规则 | 明确每个状态的可操作角色；规则未定前不自动验收或自动释放资金。退款涉及已提现资金时必须定义准备金、追偿或人工处置 | ORD-01、ORD-04、OPS-01、OPS-02 |
| D-04 | 套餐价格、权益、降级、超额服务、续费失败与佣金生效时点 | 当前页面的免费/99/199 SAR、3/10/无限服务、15%/10%/5%仅作为待确认产品输入；成交佣金在订单上快照 | PLAN-01 至 PLAN-03 |
| D-05 | 双身份是否支持，平台认证材料、人工支持职责、注销与数据保留规则 | 先完成现有 seeker/provider 角色；页面已有 `both` 痕迹但没有完整双身份流程。认证状态与支付开户状态分离；保留期限由业务负责人确认 | ACC-02、CAT-02、OPS-02、DATA-02、UX-02 |
| D-06 | AI 运行方式、可读取的业务数据、会话保留和人工升级策略 | 先完成现有 API 模型路线；本地 LoRA 作为独立实验资产。若确定生产使用本地模型，再补部署、健康检查、资源和效果验收子任务 | AI-01 至 AI-03、REL-01 |
| D-07 | 宣传内容是否包含独立需求发布、24/7 人工支持等额外服务承诺 | 核对实际交付范围；独立需求广场目前不属于已实现市场链路。若保留该承诺，必须先拆出表单、审核、匹配、关闭和通知任务，不能只保留宣传文案 | AI-03、UX-02、REL-03 |

## 3. 阶段、依赖和里程碑

| 阶段 | 任务范围 | 阶段交付与退出条件 |
| --- | --- | --- |
| M0 工程与数据基础 | BASE-01～05 | 可重建测试数据库；权限拒绝用例有效；错误提示可见；CI 有真实可运行的基础测试 |
| M1 身份与服务市场 | ACC-01～03、CAT-01～05 | 新用户能完成入驻、服务审核发布、查找与收藏；非法跨用户读写被拒绝 |
| M2 沟通与订单 | MSG-01～03、ORD-01～05 | 聊天可重试与恢复；报价金额一致；订单创建、交付、验收、评价有一致状态和权限 |
| M3 支付与提现 | PAY-01～05 | Tap 测试账户联调通过；重复请求/回调不重复收款记账；提现审批执行对账连通 |
| M4 售后与账户生命周期 | OPS-01～04、DATA-01～02 | 取消退款争议、后台操作、账务展示、导出与注销均有可追踪结果 |
| M5 客服与套餐 | AI-01～03、PLAN-01～03 | AI 与人工支持可衔接；套餐购买、续费、取消、到期和全部保留权益可验证 |
| M6 体验与文档 | UX-01～02、REL-01 | 双语/移动端/键盘流程验收；静态承诺有依据；部署与维护文档对应实际代码 |
| M7 完整验收 | REL-02～03 | 全部必需场景与故障演练通过，外部配置和运营前提落实，再决定发布 |

阶段不是整块串行等待：BASE-04/05 可与数据库盘点并行；CAT-03/04 可提前开展查询设计、界面和文案盘点，最终接线与验收仍依赖 CAT-02；支付沙箱申请可提前进行，但真实收付依赖数据、权限、金额与状态模型。AI 基础接口可先做，业务数据查询依赖 ACC/ORD；套餐界面可先做，收费依赖 PAY。

任务依赖首先表示实现顺序。跨模块验收另分两层：M2 的 ORD-02/04 可以先通过金额契约和受控付款状态夹具验证领域行为；真实付款确认须在 PAY-03 后补验，退款/账务一致性须在 OPS-01、PAY-04 后补验。记录“领域验证通过、集成待验收”，保留 In Review，不用夹具结果代替完整 Done；REL-02 汇总这些跨阶段验收。

## 4. 数据与状态设计约束

本节是各任务共享的设计约束，先在 BASE-01 / ORD-01 中对照实际数据库确认，再迁移。避免每个页面自行定义金额、角色和状态。

### 4.1 数据对象

| 类型 | 对象 | 需要补齐的约束 |
| --- | --- | --- |
| 现有对象 | `profiles`、`providers` | 身份初始化、角色唯一权威来源、认证与开户状态分离、敏感列权限、停用/注销状态 |
| 现有对象 | `services`、`favorites`、`reviews` | 服务审核与发布分离、收藏关联类型与唯一性、每订单评价唯一性、评价资格与全量聚合 |
| 现有对象 | `conversations`、`messages` | 双方成员权限、会话唯一性、消息客户端幂等 ID、参与者各自隐藏/清空/已读状态 |
| 现有对象 | `orders`、`service_history` | 订单不可变快照、状态转换、历史记录唯一性、金额精度、历史数据回填策略 |
| 现有对象 | `withdrawal_requests` | 申请、预占、审核、执行、外部终态、释放预占与审计 |
| 建议新增 | `payment_attempts`、`payment_events` | 每次支付尝试、幂等键、外部交易 ID、原始事件必要字段、验签与处理结果、重试信息 |
| 建议新增 | `ledger_entries`、`payout_attempts` | 不可重复入账、可用/冻结/已提现金额、转账尝试、对账差异与冲正记录 |
| 建议新增 | `refund_requests`、`disputes`、`order_deliveries` | 退款生命周期、售后证据、交付版本、责任人、金额上限和处置日志 |
| 建议新增 | `audit_logs`、`account_deletion_requests` | 管理员变更前后值、操作者与原因、注销各步骤结果和重试 |
| 建议新增 | `support_tickets`、AI 会话相关表 | 用户与人工客服权限、关联订单、会话保留/删除、处理状态 |
| 建议新增 | `plans`、`subscriptions`、权益及订阅事件 | 套餐规则版本、生效区间、订阅外部 ID、权益计算与生命周期 |

以上是职责划分，不要求机械创建同名表；可以复用现有对象，但必须保留这些约束和验收能力。已有数据迁移采用可兼容的扩展、回填、校验、切换步骤；未完成核对前不删旧字段，不用猜测值回填历史税额或交易状态。

每次新增个人数据对象，都要登记访问权限、导出范围、保留/删除策略及外部系统副本。M5 新增的 AI 会话、工单、通知、订阅和账单必须扩展 DATA-01/02；REL-02 再做全量数据权利验收，不能将 M4 的早期导出/注销结果直接沿用。

### 4.2 金额与外部操作不变量

- 前端显示、订单快照、支付请求、退款和账务必须使用同一版本规则；持久化沿用已核实的 SAR 精度，接口若要求其他单位只在支付适配边界转换。
- 所有金额在服务端校验有限值、正负、范围和精度；余额、实付、佣金不能直接信任客户端。
- 一张订单可有多次支付尝试，但每笔外部收款都必须被记录和对账；只允许一次满足订单的有效结算，多余到账进入异常处理，不能覆盖丢失旧交易 ID。
- 完成订单的数据库状态、历史记录和结算入账应在数据库事务/RPC 内保持一致，或使用可恢复且幂等的后续事件；重复验收不能重复加收入或项目数。
- **数据库事务不能覆盖 Tap 网络请求。** 先持久化操作意图和必要预占，再调用外部服务，再持久化结果；使用事件、幂等执行、状态查询和补偿恢复跨系统一致性。
- 超时代表结果未知，不代表失败。支付、转账、退款结果未知时保留处理中状态并查询/对账，不直接重新发起资金操作或释放预占。
- 提现可用余额以已结算收入扣除冻结、已支付、退款和相关冲正后的账务为准；不能只在页面汇总 `completed` 订单。
- 拒绝或明确失败才释放提现预占；成功出款冲销预占；重复审批、执行或回调不能重复扣减。
- 退款绑定具体已收款 Charge；可退额扣除成功退款以及处理中/结果未知退款的预占。退款、提现、争议冻结必须共享账户和相关交易的并发约束，不能各自检查后独立扣款。
- 订单关联的服务标题、计价类型、数量/工时或确认报价、金额、佣金规则和服务范围在成交时快照；后续编辑服务或切换套餐不得改写旧订单。

### 4.3 状态职责

| 领域 | 当前基础 | 建议目标与约束 |
| --- | --- | --- |
| 服务 | `is_active` | 分离审核状态与服务商发布意愿；管理员停用优先；未审核、被拒或停用服务不能直接下单 |
| 履约 | `pending → paid → awaiting_confirmation → completed` | 先保留兼容映射；明确交付版本、修改请求、取消和争议。资金异常不应被简单覆盖成履约完成 |
| 支付尝试 | 单个 `tap_charge_id` | 至少区分创建、待支付、成功、明确失败/过期、结果未知；实际状态映射以 Tap 当前能力为准 |
| 提现 | 后台审批与直接出款分离 | `requested → approved → processing → paid`；拒绝、明确失败和结果未知有独立路径；批准不代表已出款 |
| 退款 | 仅封装与展示状态 | `requested → approved → processing → succeeded`；拒绝、失败、结果未知可恢复，累计退款不得超过可退额 |
| 套餐 | 静态卡片 | 区分待支付、生效、续费异常、周期末取消和到期；前端跳转成功不能直接授予权益 |
| 注销 | profile 申请标记 | 申请、资格检查、待处理、执行、完成/失败；关联 Auth、资料、文件与法定/业务保留数据的处理结果 |

状态名称属于方案，不能在未迁移旧数据、更新所有消费者前直接替换字符串。退款与履约状态最好分别建模，特别是部分退款和已完成订单的售后。

## 5. 任务清单

### BASE-01 盘点数据库与外部配置

优先级：P0｜依赖：无｜状态：In Progress

- 实施：在获得访问条件后只读导出现有 schema、索引、约束、RLS、函数、触发器、Realtime publication、Storage bucket 与权限；登记 Auth 邮件确认/重定向配置、Tap 已开通能力和必要环境变量。区分已验证、缺失、无权读取。
- 验收：每个代码使用的表/字段/RPC/bucket 都能对应定义或待补项；列出实际孤儿记录、重复 provider、金额单位和角色来源冲突；导出不含密钥或真实用户内容。
- 入口：`supabase/migrations/`、`scripts/`、`lib/supabase/`、`.env.example`；建议新增数据库清单文档和脱敏测试数据说明。

进展记录（2026-09-09）：已新增 [BASE-01 数据库与外部配置盘点](docs/base-01-database-and-config-inventory.md)，记录代码对象、候选远端 PostgREST 契约、聚合计数、Storage 配置及确定差异。只读 CLI 复核确认候选项目仍为健康状态和 PostgreSQL 17，但当前目录未链接项目，工作区也没有数据库连接凭据。仓库 `supabase/checks/preflight.sql` 已在 Supabase Local PostgreSQL 17 只读执行，返回 1,463 行列、RLS、grants、函数、约束、索引、bucket 和 Realtime 元数据，证明采集脚本可运行；候选远端的对应 RLS、触发器、Realtime、Auth 配置和迁移历史仍未取得，继续保持 In Progress。

### BASE-02 建立可重建与可升级的数据库迁移

优先级：P0｜依赖：BASE-01｜状态：In Progress

- 实施：补全基线 schema、注册资料初始化、必要 RPC、约束、索引、Storage 和 Realtime 初始化；解决现有 `20260424000000_tap_payment_fields.sql` 先依赖表的问题，选择顺序正确的基线或明确的基线登记策略，不能仅追加一个更晚的建表迁移。空库安装与已有实例升级分别设计，避免重复除以 100 或直接重跑历史脚本。`cleanup_seeker_services.sql` 含删除操作，不作为默认初始化步骤。
- 验收：空库从零初始化成功；旧 schema 与新旧金额字段并存两类夹具升级成功；重复执行适用的初始化不会重复建资料或改变金额；迁移前后订单数、金额总额与关联关系一致。旧 100/115 等差异保留原值并生成待对账清单，不能静默修正已付款金额。
- 入口：`supabase/migrations/`、`scripts/enable_realtime.sql`、`scripts/fix_provider_associations.sql`；建议新增本地 Supabase 配置和 seed。

进展记录（2026-09-09）：已新增早于历史 Tap 迁移的 fresh-install 基础 schema，并补齐当前详情查询所需的 provider 字段、订单历史关联和核心唯一/金额约束；PGlite 空库迁移测试与 Supabase Local PostgreSQL 17 均已按顺序执行 57 份迁移，真实本地栈完成首次初始化及多次 `supabase db reset`，Tap 增量迁移重复执行不改变现有订单金额。新建库的 RLS/Storage/Realtime、统一个人资料、订单与报价幂等、完整订单快照、后台、服务商入驻、服务草稿、头像、服务审核与服务商认证、收藏、公开目录与详情投影、会话/消息分页、评价、客户订单、支付尝试/事件/对账队列/安全重关联、服务商账本与明细分页、版本化交付与私有附件、账户停用/审计、数据导出、删除申请、支持工单/回复分页、站内通知分页/Realtime、AI 知识/会话、退款、争议/私有证据、出款追踪及运营报表事务已进入版本化迁移；现有旧库 adoption 与 cents/SAR 混合升级仍待取得 schema/数据证据后完成，保持 In Progress。

### BASE-03 落实数据权限与服务端身份边界

优先级：P0｜依赖：BASE-01、BASE-02｜状态：In Review

- 实施：建立游客、需求方 A/B、服务商 A/B、管理员、受信后台任务权限矩阵。验证表和 Storage RLS、敏感列更新权限、RPC 调用权限；所有金额、身份、管理员、认证和结算字段只允许受信操作修改。若采用 `SECURITY DEFINER`，限定调用权限、`search_path` 和内部身份校验。
- 验收：直接调用 Supabase/API 也无法读取别人的会话、改订单金额、授予自己管理员/认证、越权上传删除文件；合法双方链路通过；Webhook 写入由受信服务端身份执行，不依赖浏览器 cookie，也不放宽匿名写权限。
- 入口：`lib/auth.ts`、`lib/supabase/server.ts`、`proxy.ts`、数据库策略；建议新增仅服务端使用的后台客户端。

实现记录（2026-09-09）：fresh-install 安全迁移已加入角色隔离、敏感字段触发器、服务审核约束、会话/消息/订单/评价/收藏/提现 RLS、Storage 路径规则及 Realtime 初始化。浏览器角色已撤销 `providers`、`services`、`reviews`、`favorites` 整表读取及资料、服务商、服务、会话、消息、评价、收藏的直接写入；会话、消息和订单仅保留参与方 Realtime 所需读取。公开数据通过最小服务端投影读取；本人角色、Tap 上下文、收藏、后台和直购校验均由受信服务端执行。Storage 使用限定 `search_path` 的所有权 helper。`npm run test:supabase` 已用真实 Auth/JWT、PostgREST 和 Storage API 验证本人/他人 profile RLS、整表/RPC 拒绝、公开头像/服务图片路径，以及私有交付、认证材料和争议证据的双方/管理员读取、第三方拒绝、上传归属和引用后不可删除；同时验证会话消息隔离与 Realtime 投递。双角色浏览器流程已验证服务商上传私有交付文件、需求方取得页面签名链接并读取内容，以及服务商/管理员读取私有认证材料。`npm run check:boundaries` 扫描 61 个客户端模块并禁止受限读取、业务表直接写入、客户端 RPC 和 service-role 导入。182/182 单元/API/UI、57 份 PGlite/真实 PostgreSQL 迁移、生产构建、37/37 普通 E2E、10/10 Supabase 浏览器场景及 API 网关集成通过。托管远端策略 adoption、异常网络下签名链接恢复和更多断线/多标签并发仍待 REL-02/03，保持 In Review。

### BASE-04 修复全站反馈与失败处理

优先级：P0｜依赖：无｜状态：In Review

- 实施：挂载现有 Toast 状态的显示组件；统一读取、保存、删除、重试的状态；每个数据库写入检查错误与预期受影响行数。空数据与加载失败分开，保留表单草稿，成功后再改本地状态。
- 验收：注入断网、403、约束失败和零行更新，消息、订单、服务、账户、后台分别显示可理解的错误；不出现失败却显示成功；读失败页面可重试，不伪装成零收入或空列表。
- 入口：`app/layout.tsx`、`hooks/use-toast.ts`、`components/ui/toast.tsx`、`lib/action-result.ts` 及各业务页面。

实现记录（2026-09-09）：已补全全局 Toaster 挂载、关闭按钮可访问名称和组件回归测试；后台敏感更新仅在受信事务成功后改变页面状态，读取失败会显示错误；文本/服务卡片发送失败会保留输入或面板并允许重试，发送期间防重复，清空后的读取边界也得到保留。消息内容/订单/服务、客户历史、退款、争议及证据、支持工单/回复、通知、收藏、个人资料、服务商注册/服务管理/认证、仪表盘和提现历史均已区分读取失败与空集合；统计失败统一显示不可用，不保留初始零值。公开详情区分不可见记录与后端故障；Header、首页快捷入口和后台权限壳会明确标记会话、角色或通知上下文不可用。后台总览和订单、用户、服务、服务商、认证、提现、支付异常、审计、注销、退款、争议、支持队列均有持久错误态与重试。新增共用错误卡片、Header 故障测试和公开详情故障 E2E；182/182 单元/API/UI、构建与 37/37 E2E 通过。目录故障态保留 8 秒应用超时和独立断言窗口；Playwright 改为先构建再启动生产服务器，消除开发服务器并行按需编译导致的页面导航拥塞，并完成全量复验。真实浏览器断网、403/零行注入及各表单草稿的页面级矩阵仍待 REL-02 验收，保持 In Review。

### BASE-05 建立可靠测试与 CI 基线

优先级：P1｜依赖：BASE-02（数据库测试部分）｜状态：Done

- 实施：修正登录 E2E 的宽泛定位器，断言具体表单而非页脚标题；统一维护中的 Node 与 npm 锁文件策略；CI 加入数据库测试和 E2E，保留失败 trace/截图。使用测试配置与可重复数据，不把所有外部请求成功 mock 当成集成验收。
- 验收：现有 8 个单测与 5 个 E2E 通过；权限拒绝测试可以真实失败；测试无运行顺序依赖；类型检查与构建可在干净检出中执行。
- 入口：`.github/workflows/ci.yml`、`playwright.config.ts`、`vitest.config.ts`、`e2e/auth.spec.ts`、`tests/`、`package.json`。

完成记录（2026-09-09）：登录 E2E 已改为断言具体表单控件，新增 Toast、认证表单、安全回跳、管理员、订单、Webhook、服务商入驻、服务草稿、头像、审核与服务商认证、收藏、目录、消息/会话分页、评价、订单历史与快照、支付尝试/事件/对账任务、账本/明细、版本化交付/私有文件、账户停用/审计、数据权利、AI 接口/会话、支持工单/回复分页、通知分页/Realtime、退款、争议、出款追踪、运营统计、主题/RTL/窄屏和公开详情故障回归，并把 Playwright 从可能误复用其他应用的 3000 端口迁至独立 3107 端口；测试专用 Supabase 占位配置已固化到 Playwright WebServer，直接运行文档命令无需依赖隐藏 shell 状态。目录查询增加 8 秒服务端超时，后端悬挂会稳定进入可重试错误；Playwright 在独立 3107 端口先构建再启动生产服务器，避免并行测试触发开发服务器编译拥塞。CI 已加入客户端数据边界检查、PGlite 数据库测试、Chromium E2E 和失败产物保留，并移除遗留 Stripe 构建变量。npm/package-lock 是唯一安装来源，长期失配的 pnpm lock 已移除，`.nvmrc` 和 engines 固定受支持 Node 范围。使用 Node 24.19.0 从 `npm ci` 重新安装后，182/182 单元/API/UI 测试、57 份数据库迁移、37/37 E2E、客户端边界、TypeScript 与构建通过，Lint 为 0 错误/0 警告。真实 Supabase 网关、多连接并发和外部沙箱属于 BASE-03/PAY/REL 的集成验收范围。

### ACC-01 补全认证生命周期

优先级：P1｜依赖：BASE-02、BASE-03、BASE-04｜状态：In Review

- 实施：完善注册后的邮件确认、重复注册、确认链接失效与重发；实现找回密码、恢复回调与重置页；登录后恢复站内目标路径，拒绝外域重定向；处理会话过期与退出。
- 验收：新用户从邮件确认到登录可走通；重置链接一次性/失效场景明确；重置成功后旧密码失效；从带查询参数的业务页登录能返回原操作上下文；`redirect` 不能跳往任意外域。
- 入口：`app/auth/login/page.tsx`、`app/auth/signup/page.tsx`、`proxy.ts`；建议新增找回密码、重置及 Auth 回调路由。

实现记录（2026-09-09）：已实现站内安全 `next`、邮箱确认 code exchange、找回密码、重置页、失效链接反馈和注册后确认状态；外域、反斜杠和控制字符回跳均被拒绝，Proxy 覆盖全部受保护及后台路由。Supabase Local 启用邮件确认、8 位密码、安全改密和明确 callback allowlist；API 级测试通过真实 Mailpit/PKCE 完成注册邮件、允许域回跳、code exchange、确认链接一次性失效、recovery 邮件、recovery code exchange、改密及旧密码失效。浏览器集成进一步从真实注册表单点击邮件链接进入 Next.js `/auth/callback`，完成退出、找回、重置及新旧密码登录；测试发现 `request.nextUrl.origin` 在代理下会从 `127.0.0.1` 变成 `localhost` 并丢失 PKCE cookie，现改用受信 `NEXT_PUBLIC_SITE_URL` 固定回调 origin。托管环境 SMTP、邮件送达率、正式域名 allowlist 和会话过期仍待 REL-03 验收，状态转 In Review。

### ACC-02 统一角色与服务商入驻

优先级：P0｜依赖：BASE-02、BASE-03、ACC-01、D-05｜状态：In Review

- 实施：统一 `profiles.role`、`user_type`、auth metadata 的权威关系；注册 provider 意向、provider 资料存在、平台认证、支付开户分别表达；入驻操作可重试且不重复创建，处理写资料成功但更新角色失败的恢复。
- 验收：新注册 seeker/provider 均进入正确页面；重复提交仅一份资料；部分失败重试可完成；修改 auth metadata 不能自行获得管理员或认证；不支持的双身份入口不出现。
- 入口：`app/register/provider/page.tsx`、`app/auth/signup/page.tsx`、`components/header.tsx`、`app/page.tsx`、`lib/auth.ts`、`proxy.ts`。

实现记录（2026-09-09）：应用角色读取已统一为 `profiles.role`，注册 metadata 仅作为数据库创建 profile 时的初始意向；服务商资料通过受信幂等事务写入，数据库锁定 profile、要求 provider 角色并保留认证/统计/支付字段，重复提交返回同一 provider。除单元/PGlite 的重复提交、失败和 metadata 篡改测试外，Supabase 浏览器集成已验证 provider 注册邮件确认后恢复 `/register/provider`，三步填写双语资料、分类、技能和价格，经 Server Action 创建唯一 provider 后进入 Dashboard。真实旧库 `user_type`/role 冲突 adoption 和 D-05 双身份决策仍待处理，状态转 In Review。

### ACC-03 完善资料、头像与改密

优先级：P1｜依赖：BASE-03、BASE-04、ACC-01｜状态：In Review

- 实施：统一资料字段存储与双语展示；改密页面若要求旧密码则真正重新认证，并遵守 Auth 的服务端验证配置；头像先安全上传、更新引用成功后再清理旧文件，provider 同步失败可恢复。
- 验收：刷新后资料一致；旧密码错误不能按页面声称的验证流程改密；新头像上传/落库失败旧头像仍可用；跨用户文件操作被拒绝；文件类型、体积和路径校验生效。
- 入口：`app/profile/page.tsx`、`app/provider/[id]/page.tsx`、Storage 策略。

实现记录（2026-09-08）：资料页改密先用当前邮箱与旧密码重新认证，再更新新密码；密码长度与重置页统一为至少 8 位，并补齐 autocomplete。头像上传唯一新对象后，以受信事务同步 profile/provider 引用，再清理旧对象；保存失败删除新对象并保留旧引用，清理失败明确警告。`location` 与 `bio` 已从可变且分步写入的 Auth metadata 迁入 `profiles`，迁移会回填旧 metadata，运行时只读数据库权威字段；姓名、电话、地区和简介由一个受信 RPC 校验并原子保存，数据导出自动包含统一字段。数据库已验证浏览器直写被拒、长度/空值处理和导出值，Action 覆盖正常与无效输入。头像 URL 来源、MIME/大小和跨用户 Storage 路径已有应用、单元及数据库校验。真实旧 metadata 回填、旧密码失效和 Storage 网关仍待 REL-02/03 验收，保持 In Review。

### CAT-01 完善服务 CRUD 与图片生命周期

优先级：P1｜依赖：BASE-03、BASE-04、ACC-02｜状态：In Review

- 实施：创建与编辑共享必填、价格、计价类型、图片数量规则，并在服务端校验；先创建可恢复草稿，必要图片成功关联后再提交发布/审核；处理部分上传失败、移除图片和删除服务后的孤儿文件。
- 验收：图片失败不发布空壳服务或假报成功；编辑不能绕过创建校验；用户可继续失败的草稿；服务删除不破坏已成交订单快照；重复清理不会删除仍被引用或其他用户的文件。
- 入口：`app/my-services/page.tsx`、`services` 与 Storage；建议新增服务写入 Action/RPC。

实现记录（2026-09-09）：服务创建、编辑和删除已迁到受信事务并统一服务端字段、类别、计价、两位小数、特性和图片引用校验；“我的服务”列表改为受信双语搜索及创建时间/价格/语言名称三类排序，每种排序使用对应值与 ID 的复合游标，切换语言或排序不会复用错误边界。创建先保存 inactive 草稿，图片全部成功后才送审，部分/全部失败保留可编辑草稿并明确提示。编辑成功后清理移除图片，事务失败清理本次新上传，删除受订单外键保护并清理关联图片。上传路径按真实 MIME 使用 UUID 扩展，取消编辑会释放本地预览 URL。Storage 删除失败时先写当前设备离线兜底，恢复连接后同步到服务端 `service_image_cleanup_jobs`；队列按服务商归属校验路径、拒绝仍被服务引用的文件，并阻止待清理或已清理路径重新挂回服务。任意设备可查看待清理总数并触发受信服务端批量删除；删除失败保留任务、增加尝试次数，成功后才标记完成。队列纳入数据导出，数据库已验证越权拒绝、引用保护、失败/成功状态和重新引用拒绝；Action 覆盖跨设备读取、删除及失败保留，本地兜底继续过滤重复、跨 provider 与目录穿越项。服务/头像 bucket 的 MIME、大小和归属 RLS 已在新建库验证；Supabase Local 浏览器已验证真实 PNG 上传、引用、换图、旧对象删除、编辑后重新审核、下架和再发布。服务端只允许配置原点的 HTTPS 图片 URL，另对 `localhost`、`127.0.0.1` 和 `::1` 的本地 HTTP 开发原点开放。Storage 网关故障、多文件部分成功和未入队孤儿扫描仍待 REL-02/03 验收，保持 In Review。

### CAT-02 实现服务审核与认证管理

优先级：P0｜依赖：BASE-03、ACC-02、CAT-01、D-05｜状态：In Review

- 实施：分开审核、服务商上下架和管理员停用；创建默认待审核/草稿；定义哪些编辑需要重新审核及旧版本如何展示。平台认证有材料、审核人、结论与撤销记录，材料使用私有存储或受控外部流程。
- 验收：未经批准不能发布/下单；被停用服务不能自行重新激活；审核失败有原因和重新提交入口；匿名用户看不到认证材料；展示徽章严格对应审核结果。
- 入口：`app/admin/services/page.tsx`、`app/admin/providers/page.tsx`、`app/my-services/page.tsx`、`app/register/provider/page.tsx`。

实现记录（2026-09-09）：服务已分离草稿、待审、通过、拒绝和管理员暂停状态，并以数据库约束保证公开 `is_active` 只在“审核通过且服务商愿意发布”时成立。服务商编辑回到草稿、完整图片后送审，可查看拒绝原因并在通过后自行暂停/恢复；管理员界面使用受信服务端双语搜索、审核状态过滤和创建时间/ID 复合游标分页，待审总数不受当前页影响；管理员可填写拒绝原因、批准或暂停，所有决定记录审核人、时间和不可删除审计。服务生命周期已通过 Supabase Local 双角色浏览器验证创建送审、管理员批准、公开可见、编辑重新送审、再次批准、暂停及恢复发布。服务商认证改为独立申请流程：服务商最多上传 5 个 PDF/图片材料到私有 `provider-verification` bucket，可用稳定请求 ID 重试、按复合游标查看完整历史并取消待审申请；管理员队列同样可继续加载旧申请，只有本人及管理员可读取签名文件。管理员必须基于申请填写审批原因，批准、拒绝和撤销均记录审计并发送通知；原先直接切换认证字段的后台动作已禁用。认证申请与文件索引纳入数据导出，数据库验证了幂等申请、RLS 隔离、审批及引用文件不可删除；Supabase Local 双角色浏览器已验证服务商上传并读取 PDF、管理员读取同一签名文件、填写审批理由和批准后状态回显。正式材料清单和复审周期仍待 D-05 确认，保持 In Review。

### CAT-03 完善市场查询与分页

优先级：P1｜依赖：BASE-02、BASE-04、CAT-02｜状态：In Review

- 实施：服务端完成分类、关键词、价格/评分排序和分页，定义稳定次序；查询参数与 URL 同步；服务商工作台、历史与后台列表采用相同分页原则，不全量下载后切片。
- 验收：多页数据不遗漏重复；刷新和浏览器前进后退保留筛选；中英文/阿拉伯文搜索有夹具；下架服务不进入公共结果；数据库失败显示重试。
- 入口：`app/services/seeker/page.tsx`、`components/header.tsx`、相关索引及查询函数。

实现记录（2026-09-08）：公开目录已改为受信数据库查询，关键词同时覆盖阿英名称和说明，分类、价格、评分及时间排序在数据库完成，并以创建时间和 ID 提供稳定次序；返回总数和 12 条分页数据，客户端不再全量下载。搜索采用 300ms 防抖，查询、分类、排序、页码同步到 URL，超范围页回到第一页，失败有独立重试状态。数据库和 Action 测试已通过，待采用迁移后的真实 PostgREST/页面前进后退与多页数据验收后转 Done。

### CAT-04 修正详情与公开资料真实性

优先级：P1｜依赖：CAT-02、BASE-04｜状态：In Review

- 实施：移除无条件认证/Pro Seller、零评分回退 5.0、固定最近交付时间；服务 FAQ、需求说明和交付流程按真实服务提供；未配置内容隐藏或标注未知。公共详情与下单共用可见/可交易约束。
- 验收：新服务显示暂无评价；未认证服务商没有认证徽章；停用/删除服务直链返回明确状态；不同品类不出现统一 Logo Design 模板；名称、图片、统计来自对应记录。
- 入口：`app/services/[id]/page.tsx`、`app/provider/[id]/page.tsx`。

实现记录（2026-09-08）：详情页已移除零评价回退五星、固定最近交付、无条件认证/Pro Seller、Logo Design FAQ/标签和品类限定需求，响应时间缺失显示未提供；无评价显示明确空态，图库 fallback 使用受支持的本地资源。已用候选远端未认证、零评价服务做桌面渲染检查，未出现认证、专业卖家或一天前交付声明。评分总数、平均值和五星分布现由全量数据库聚合提供，评论列表独立使用 10 条复合游标分页。公开 RLS 已验证服务编辑回草稿后匿名直链不可读；服务和服务商页面分别显示“不可用”状态，网络/数据库故障显示可重试错误；服务商主页的可售服务由受信查询统一校验 approved、发布意愿及双方账户状态，并以创建时间/ID 复合游标分页，全量服务数不再等于当前已加载数量。关联服务、服务列表和收藏状态失败不会伪装为空。公开失败路径浏览器回归通过。真实 Supabase 下架直链、登录所有者视图及阿英/移动端渲染仍待 REL-02 验收，保持 In Review。

### CAT-05 补全收藏

优先级：P1｜依赖：BASE-02、BASE-03、BASE-04、CAT-04｜状态：In Review

- 实施：先按现有 `favorites` schema 确认收藏对象是服务商还是服务，保持现有列表语义；增加对应卡片/详情入口、添加/取消及列表状态同步，建立用户与目标唯一约束。需要新增另一收藏类型时独立迁移。
- 验收：添加后刷新和重新登录仍存在；重复点击不产生重复记录；取消失败恢复原状态；游客操作可登录后继续；目标下架/删除不会导致整页崩溃；用户互不可读写收藏。
- 入口：`app/favorites/page.tsx`、市场及对应详情页、`favorites` 策略。

实现记录（2026-09-08）：已确认收藏对象为 provider，并在服务商主页加入双语添加/取消入口和登录后回跳；收藏写入通过受信幂等事务验证目标有效、禁止收藏自己并依赖唯一约束去重。收藏列表通过一次受信数据库查询返回收藏及最小服务商资料，按收藏时间/ID 复合游标分页，不再依赖两次可能分别截断的浏览器查询。读取或翻页失败保留已有列表；失效 provider 保留历史卡片、标记不可用并关闭联系入口，取消失败不改变本地状态，也不再无条件显示 Professional 徽章；跨用户读取由显式身份检查和 RLS 隔离。单元及数据库正常/重复/越权场景通过，待真实登录刷新和多页面状态验收后转 Done。

### MSG-01 可靠发送文本、服务卡片与报价消息

优先级：P1｜依赖：BASE-03、BASE-04、ACC-02｜状态：In Review

- 实施：添加客户端消息 ID 和发送中/失败/成功状态；数据库确认前保留内容，支持同 ID 重试；会话时间与消息持久化一致；服务卡片内容使用结构化类型和校验，不依赖不受约束的字符串解释。
- 验收：断网后草稿/失败消息可恢复；重复发送请求只落一条消息；文本、服务卡片、订单消息都显示失败；发送者不能伪造其他用户或向非成员会话写入。
- 入口：`app/messages/page.tsx`、`messages`、`conversations`；建议新增消息写入 Action/RPC。

实现记录（2026-09-08）：文本和服务卡片均通过受信事务发送；客户端为一次逻辑发送保留 UUID，数据库以会话、发送者和请求 ID 唯一约束去重，重复请求返回原消息，不同内容复用同 ID 会被拒绝。消息写入、会话时间和收件方隐藏恢复原子完成；服务卡片由数据库读取当前可售服务生成，客户端不能伪造名称或价格。文本/面板在失败时保留并可同 ID 重试，发送期间防重复。单元和数据库重复/越权/篡改场景通过，待真实双端断网恢复验收后转 Done。

### MSG-02 补全隐藏、恢复、清空与置顶

优先级：P1｜依赖：MSG-01｜状态：In Review

- 实施：双方都可查看和恢复自己的隐藏会话；明确新消息提醒/恢复规则；清空只影响当前用户可见边界，所有读取路径包括发送服务卡片后的刷新都遵守 `cleared_at`；置顶/隐藏互不修改另一方偏好。
- 验收：服务商隐藏后仍能发现新消息并恢复；清空后重载、切换、发卡片不复现旧消息；一方清空不删另一方历史；失败操作不改变本地结果。
- 入口：`app/messages/page.tsx` 中会话筛选、`fetchMessages`、`sendServiceCard`、偏好更新。

实现记录（2026-09-08）：置顶、隐藏、恢复和清空已统一到按参与者写入的受信事务；消息和会话订单查询都在数据库应用当前用户的 cleared_at，清空后旧消息与订单卡片不会恢复，且不影响另一方历史。双方均可切换到隐藏会话列表主动恢复；对方新消息会原子恢复收件方会话，发送者不能修改另一方偏好。数据库双向恢复、清空隔离和越权场景通过，待真实 Realtime 双端验收后转 Done。

### MSG-03 完善实时恢复、历史分页与未读

优先级：P1｜依赖：MSG-01、MSG-02、BASE-02｜状态：In Review

- 实施：会话和历史消息分页、稳定排序；显示订阅连接状态；重连后按游标补拉并去重；实现未读数量和已读边界，处理多标签页及组件卸载。
- 验收：离线期间的消息重连后补齐；快速切会话不串消息；历史翻页不跳位置、不重复；未读数在双方、多标签页和刷新后保持一致；登录过期不会继续订阅私人会话。
- 入口：`app/messages/page.tsx`、`components/header.tsx`、Realtime publication、消息索引。

实现记录（2026-09-09）：消息历史与会话订单均改为服务端 50 条复合游标分页，“加载更早活动”同时补齐两类时间线数据并保持滚动位置、去重；服务卡片候选复用公开可售服务分页，不再下载服务整表。会话列表也改为受信服务端双语搜索及 50 条复合游标分页，置顶、最后活动时间和 ID 保证稳定次序，隐藏状态、对方公开资料、清空边界及未读数由同一查询生成。已读事务会触发会话订阅刷新其他标签页的未读徽标；聊天头展示连接/离线状态，Realtime 重新订阅后补拉最新页，连接失效且会话无效时主动退订并返回登录。数据库已验证跨页顺序、隐藏集合、清空后未读归零和浏览器角色拒绝，Action 已验证双语查询参数与游标。182/182 单元/API/UI、57 份迁移、构建与 37/37 E2E 通过。真实 Supabase 双端断线、乱序、多标签页和大量会话滚动位置仍待 REL-02 验收，保持 In Review。

### ORD-01 统一订单状态与操作权限

优先级：P0｜依赖：BASE-02、BASE-03、D-03｜状态：In Progress

- 实施：列出每个状态、允许动作、操作者、前置金额/支付条件及失败行为；履约、支付、退款分开建模；状态变更使用受控 Action/RPC 和条件更新。同步消息、工作台、历史和后台消费者。
- 验收：需求方不能代服务商交付，服务商不能代需求方验收；未支付不能交付；重复动作可返回已有结果但不重复执行；过期页面或并发操作不能越过状态约束。
- 入口：`app/actions/orders.ts`、`components/orders-table.tsx`、`app/messages/page.tsx`、`app/history/page.tsx`。

进展记录（2026-09-09）：报价建单、固定价直购、付款尝试、到账、取消、版本化交付、修改请求、再次交付和客户验收已集中到受信数据库事务，并在事务内重验操作者与旧状态；重复直购、付款、交付、验收和晚到回调已做幂等测试。Supabase Local 双浏览器上下文已验证付款后的服务商交付、需求方修改请求、再次交付和验收，并要求状态通过 Realtime 在另一端无刷新出现。拒收转售后、争议及最终 D-03 超时/次数规则仍待实现，保持 In Progress。

### ORD-02 统一计价与订单快照

优先级：P0｜依赖：ORD-01、D-01、CAT-01｜状态：In Progress

- 实施：统一报价与下单服务端计算；固定价、按小时、起步价分别要求固定总价、确认工时/数量、或双方确认报价；保存金额拆分、规则版本、服务范围、有效期和关联服务。旧订单保留原实付证据，不猜测补税。
- 验收：100 SAR 示例按最终规则在报价、订单、支付、收据、退款和账务一致；覆盖小数、边界金额和恶意客户端值；按小时/起步价不能直接被当最终固定总额；服务改价不影响旧单。
- 入口：`lib/tap.ts`、`components/create-order-dialog.tsx`、`app/actions/orders.ts`、服务详情。

进展记录（2026-09-09）：已移除报价窗口中尚未确定却未进入订单/支付的 VAT 展示，使当前报价总额与订单金额一致；服务端限制两位小数和金额范围；非固定价服务改为请求报价，并由数据库拒绝直购。新增不可漂移的 `service_snapshot` 与 `pricing_snapshot`：固定价直购会冻结成交时的双语内容、类别、目录价格、计价类型、交付说明、功能项和图片；服务商报价会同时保存协商内容及关联目录版本。快照记录当次总额、平台费、服务商金额、币种和实际采用的 15% 旧规则；未确认的税额、按小时单价及数量保持显式空值，旧订单只从订单原字段建立 `legacy-commission-unverified` 证据，不用当前服务或税务假设回填。数据库约束阻止快照与订单金额/名称漂移，并已验证两类建单和篡改拒绝。D-01 最终税务/佣金规则与按小时数量仍待确认，保持 In Progress。

### ORD-03 保证会话与订单创建一致

优先级：P0｜依赖：ORD-01、ORD-02、CAT-02、MSG-01｜状态：In Review

- 实施：直接下单和聊天报价使用同一订单契约；校验服务/服务商可交易、服务归属、双方身份、禁止自购和报价是否有效；用幂等键及数据库约束处理会话/订单并发创建，失败可回滚或恢复。
- 验收：双击、网络重试和同时打开两个页面只形成一次预期订单；不能用别人的 serviceId 伪装报价；被停用服务在 API 层也拒绝；建会话成功但建单失败后可重试且不产生孤儿订单。
- 入口：`createOrder`、`createDirectOrder`、`components/create-order-dialog.tsx`、数据库函数。

实现记录（2026-09-08）：直购在同一数据库函数内重读可交易固定价服务、禁止自购并复用/创建唯一会话；新增只覆盖直购来源的待付款订单唯一约束和冲突后重读逻辑，两个并发入口不能各建一单，也不会错误复用同服务的定制报价。聊天报价在事务内校验会话、服务归属、已审核可售状态和提供者身份；客户端为同一逻辑提交保留请求 UUID，数据库按服务商与请求 ID 唯一去重，响应丢失后可返回原订单，若复用 ID 且内容不同则拒绝。消息发送已有独立幂等键，服务编辑回草稿后所有 API 入口拒绝交易。数据库已验证重复报价、内容冲突、直购唯一约束、浏览器直调拒绝及定制报价与直购并存。真实多连接并发、响应中断恢复和双页面浏览器验收仍待 REL-02，保持 In Review。

### ORD-04 补全交付、修改与验收

优先级：P1｜依赖：ORD-01、ORD-03、MSG-01、D-03｜状态：In Review

- 实施：交付有说明和附件/链接、版本、时间；客户可验收或按规则请求修改；履约确认、历史记录和项目数更新保持一致且可重试。明确逾期提醒与人工处理入口。
- 验收：完整订单经历付款状态确认、交付、修改、再次交付、验收；重复验收只产生一份历史和一次计数；历史/统计写入失败可恢复；交付文件只有授权参与者可访问。
- 入口：`app/actions/orders.ts`、`app/messages/page.tsx`、`components/orders-table.tsx`、`service_history`；建议新增交付记录。

实现记录（2026-09-09）：已新增版本化 `order_deliveries` 和显式 `revision_requested` 状态；服务商可提交交付说明、最多 5 个 HTTPS 成果链接及最多 5 个私有成果文件。`order-deliveries` 私有 bucket 限制单文件 25MB 和 PDF/ZIP/文本/图片/Office MIME，路径绑定订单、服务商与 request ID；只有订单双方和管理员可读，未引用文件可由上传者清理，写入版本后的文件不能删除。文件名、MIME、大小和路径随交付版本及订单最新快照保存，页面通过 10 分钟签名 URL 下载。客户端用稳定 request ID/路径处理部分上传和响应丢失；结果未知时锁定表单并先查询已存在交付，避免重复版本或覆盖文件。客户可接受或填写修改原因，服务商可按原因再次交付；最终接受、交付版本、历史、项目计数和账本结算保持同一事务。数据库已验证缺失文件拒绝、参与者/第三方隔离、引用后删除拒绝、重复请求恢复及数据导出，组件测试覆盖上传、签名下载和未知结果恢复。Supabase Local 双角色浏览器已实际上传并读取两版私有文本交付、展示修改原因、再次交付和验收。修改次数/时限、逾期提醒及异常网络恢复仍依赖 D-03/REL-02，保持 In Review。

### ORD-05 完善历史与评价资格

优先级：P1｜依赖：ORD-04、CAT-04、BASE-03｜状态：In Review

- 实施：历史列表覆盖待验收等相关状态；评价仅允许符合最终履约规则的本人订单，每订单唯一；编辑/删除检查结果。评价列表分页与评分总数、平均值、分布的全量聚合分开实现。
- 验收：待验收订单不消失；未付款/未验收不能通过直接 API 评价；重复提交不重复计分；至少 11 条不同评分夹具下全量统计准确；删除/修改后各页统计一致。
- 入口：`app/history/page.tsx`、`app/services/[id]/page.tsx`、`app/my-services/page.tsx`、评价约束/聚合。

实现记录（2026-09-09）：历史页已升级为分页“我的订单”，覆盖 pending、paid、awaiting_confirmation、completed、cancelled 和 refunded，服务删除后继续使用订单快照；评价入口只在 completed 状态显示。评价新增/编辑统一为订单幂等保存事务，数据库重验本人、完成状态和 service 关联，每单唯一；删除同样检查所有权。详情摘要使用全量聚合，评论独立分页，数据库以 11 条不同评分验证总数/分布不受首屏 10 条影响；服务商工作台评价弹窗也复用同一受信聚合与复合游标，不再直接读取全部评价后另查用户资料，翻页失败保留已加载评论。Supabase Local 浏览器已验证验收后历史页出现评价入口、保存四星评价并回读数据库。服务商侧评论翻页以及评价编辑/删除的真实浏览器交互仍待 REL-02 验收，保持 In Review。

### PAY-01 接通真实支付账户入驻

优先级：P0｜依赖：BASE-03、ACC-02、D-02｜状态：In Progress

- 实施：按已确认的 Tap 商户能力替换 placeholder；提供真实入驻链接/受控开户流程、状态同步、失败和补件入口；区分平台认证、收款能力、提现能力；清点旧占位 destination。
- 验收：测试服务商可从未连接走到外部验证通过；过期链接可重新生成；未完成者不能调用提现；不能把本地布尔值或 URL 参数当作开户完成；占位 ID 不进入真实转账。
- 入口：`app/actions/tap-connect.ts`、`app/dashboard/page.tsx`、provider 支付字段。

进展记录（2026-09-09）：已按 [Tap Marketplace 能力与接入边界](docs/tap-marketplace-capability-matrix.md) 区分 Marketplace key 与普通 Merchant Charge key，并接入 `GET /v2/destination/{destination_id}` 的服务端状态复核。provider 新增独立的收款能力、提现能力、最近核验时间和来源字段；外部状态通过仅 service-role 可调用的事务同步，destination ID 必须与已绑定值一致，浏览器不能自行写成 active。Dashboard 不再用本地 `tap_onboarding_completed` 直接显示已连接；提现前必须重新查询 Tap，只有 destination 为 active 且响应明确给出 payout=true，并在五分钟内写入核验结果，数据库才允许预占余额。旧的 `active/true` 标志、占位 destination、Marketplace 开关关闭、响应 ID 不一致、仅收款可用但 payout 未启用都会拒绝提现。当前仍未实现 KSA 自助开户：官方流程要求真实 KYC/CR、National ID、IBAN 与文件，测试环境也校验真实性，且需要 Tap 分配的 Marketplace MID/keys、最终材料清单和 onboarding webhook 契约；`createConnectAccount`/`createAccountLink` 继续显式不可用，避免生成假链接。取得 D-02 与隔离 sandbox 后再完成 lead、文件、account、补件和 webhook 链路，保持 In Progress。

### PAY-02 实现可追踪且可重试的支付尝试

优先级：P0｜依赖：ORD-02、ORD-03、D-02｜状态：In Review

- 实施：持久化支付尝试和稳定操作 ID；复用有效待支付尝试，明确何时允许新建；保存所有外部 Charge；创建前后异常均可恢复。按 Tap 支持的幂等/查询能力设计，不能假设任意接口支持自定义幂等头。
- 验收：重复点击只返回同一次有效支付；创建成功但响应/落库失败可查询恢复；旧 Charge 意外到账也不丢失，进入重复到账处理；页面回跳不直接把订单标为已付款。
- 入口：`app/actions/orders.ts`、`lib/tap.ts`、`app/messages/page.tsx`；建议新增 `payment_attempts`。

实现记录（2026-09-09）：`payment_attempts` 使用订单锁和稳定 attempt ID，Charge metadata、状态、交易号及 checkout URL 独立保存；重复点击复用同一活动尝试，明确失败后才允许新建，未知结果保持对账锁定。状态映射覆盖 `INITIATED`、`AUTHORIZED`、`IN_PROGRESS`、`ABANDONED`、`TIMEDOUT` 和 `UNKNOWN`；活动唯一约束排除 captured 终态，数据库已验证超时重试后旧 Charge 晚到仍能绑定原 attempt 并结算。后台“Recover Charge”可对响应丢失的 creating attempt 输入 TapOS Charge ID 和证据，服务端实时查询 Tap，Action 与数据库事务双重核对 order/attempt metadata、金额、币种和 ID，再记录事件、执行处理器并审计；不匹配不能绑定。代码实现与故障恢复验证已完成，真实 Tap sandbox 的 metadata 检索、显式 expiry 和网络中断场景待 D-02/REL-03，状态转 In Review。

### PAY-03 修正回调、到账核验与对账恢复

优先级：P0｜依赖：BASE-03、PAY-02｜状态：In Review

- 实施：按 Tap 官方字段顺序、金额格式与实际密钥配置计算签名；受信服务端持久化事件后才确认接收，持久化失败返回可重试错误；校验交易与订单的关联、金额、币种、状态，不匹配事件隔离并告警；回调与主动查询共用幂等处理器。定期扫描超时/未知交易，后台可查异常并重试对账。
- 验收：官方规则合法签名通过，篡改金额/状态失败；重复与乱序回调不会回退终态或重复入账；关闭浏览器或回调始终未抵达也能通过对账恢复；事件持久化失败后重投不丢事件；零行更新区分“已处理”与“订单不存在/权限失败”；Tap 测试环境真实回调通过。
- 入口：`app/api/webhooks/tap/route.ts`、`lib/tap.ts`、`verifyPayment`；建议新增事件表、服务端对账任务与故障列表。

实现记录（2026-09-09）：已按 Tap 字段拼接与 SAR 两位小数规则验签，篡改金额测试会被拒绝；Webhook、checkout 返回与主动查询共用 `payment_events` 记录/处理器，事件成功持久化后才确认接收，数据库失败返回 500 供重试。重复事件幂等，乱序失败不会回退已捕获状态，金额/币种/订单或 Charge 不匹配进入隔离而不丢失。后台展示按更新时间/ID 分页的全部 creating/pending/unknown attempt，最旧优先并附 Charge、订单、金额和最新事件；payment event 异常列表也改为 received_at/ID 复合游标分页，可分别筛选 pending 与 quarantined，并显示两类全量计数；无 Charge ID 明确要求人工恢复。管理员可对 quarantined 事件填写 order、attempt 和审计理由，数据库仅在签名、Charge、金额、币种及关联全部一致时重新处理，不能强制改金额或标成功。`TAP_PAYMENT_RECONCILIATION_ENABLED` 控制单笔与批量只读查询，`POST /api/jobs/payment-reconciliation` 另受 24 字符以上 Bearer secret、5～1440 分钟 age 和每批 25 条限制；任务绝不创建 Charge，返回汇总而不暴露标识。数据库已验证非管理员拒绝、错误关联拒绝、成功重关联/审计和开放队列；Action/API 覆盖门控、鉴权、CAPTURED 恢复、详情不匹配与部分失败。真实 Tap sandbox、部署调度器和告警接收者仍待 REL-03，保持 In Review。

### PAY-04 建立结算账务与提现申请审批

优先级：P0｜依赖：ORD-04、PAY-01、PAY-03、D-03｜状态：In Review

- 实施：订单结算生成唯一账务记录，明确待结算、可提现、冻结和已支付金额；提现申请在数据库内原子校验并预占额度；后台审批写操作者/理由，拒绝释放预占，批准进入待执行。
- 验收：两个并发申请不能超过同一余额；重复验收不重复入账；未入驻、未结算、争议冻结资金不可提现；管理员批准后仍显示待出款；可用余额与流水逐笔核对一致。旧 `approved/completed` 提现逐笔核实渠道结果后建立期初账和尝试记录，无法证实的冻结待对账，不能直接信任旧状态。
- 入口：`app/actions/tap-connect.ts`、`app/dashboard/page.tsx`、`app/admin/withdrawals/page.tsx`；建议新增账务表与预占 RPC。

实现记录（2026-09-09）：订单验收在同一事务写唯一结算分录；提现申请在 provider 锁内按账本可用余额原子转为预留，拒绝释放，批准保持待出款，外部结果由 PAY-05 的尝试/证据流程处理。退款申请、失败、成功及争议开启、裁决退款、继续履约都会在订单锁下写对应 hold/release/refund 分录，开放退款/争议资金不可提现；旧 completed 提现仅冻结待核实，不推断已出款。服务商仪表盘从同一账本聚合 available/reserved/paid/total，并新增 50 条复合游标逐笔账本及提现历史，显示结算、预留、释放、退款、争议和出款对各余额的影响、订单/提现引用及备注；失败保留已加载记录，不显示假零。数据库以 16 条分录核对 18 available、0 reserved、50 paid、68 total，验证跨页无重复和浏览器直调拒绝。任意人工增减余额仍未开放，因为缺少双人审批和会计凭证规则；真实旧账采用与外部出款核验待 REL-03，保持 In Review。

### PAY-05 接通提现执行、失败恢复与明细

优先级：P0｜依赖：PAY-04｜状态：In Review

- 实施：受信执行器领取已批准申请，保存转账尝试；核验真实外部终态后才标已支付；超时保留未知结果并查询，不自动二次转账；明确失败才释放/恢复额度。提供双方状态明细和管理员对账入口。
- 验收：重复审批/执行只出款一次；进程在外部成功、本地未记录时崩溃可恢复；处理中转账不显示 completed；失败/拒绝的余额正确恢复；服务商可看到金额、手续费、状态、时间和参考号。
- 入口：`createPayout`、`lib/tap.ts`、`app/admin/withdrawals/page.tsx`、`app/dashboard/page.tsx`；建议新增 `payout_attempts` 与任务执行入口。

实现记录（2026-09-08）：对照 Tap Marketplace 当前文档后，已移除未使用且未证实可用的 `/transfers` 封装；文档说明出款通常按协议自动进行，或在 Tap dashboard 手动发起，公开 Payout API用于查询状态/报表。批准后的提现现在通过 `payout_attempts` 记录 `tap_auto`/`tap_dashboard`、外部 reference、操作者和审计；开放退款/争议会阻止进入出款追踪。`unknown` 保持预留，明确失败释放，只有在独立 `TAP_PAYOUT_RECONCILIATION_ENABLED` 开关开启并提供核对证据后才允许把预留转为 paid。服务商仪表盘显示申请金额、状态、方式、reference 和失败原因；服务商历史与管理员全状态队列均改为请求时间/ID 复合游标分页，管理员页面保留全局待审数量并可登记外部发起及结果。提交成功文案只承诺进入审核和可追踪状态，不再虚构 1–3 个工作日到账。数据库已验证重复追踪、退款互斥、unknown 后 PAID_OUT、账本预留/已支付、双方分页、权限、通知、注销阻塞和导出。真实 Tap 自动/手动出款、Payout report 查询、商户协议/KYC 和沙箱核对仍待 D-02/REL-03，保持 In Review。

### OPS-01 完成取消、全额及部分退款

优先级：P0｜依赖：ORD-01、PAY-03、PAY-04、D-01、D-03｜状态：In Review

- 实施：区分未支付取消、已支付退款申请和外部退款；处理取消与到账竞争；退款绑定具体已收款 Charge，事务内按“已成功退款 + 处理中/未知退款预占”计算可退额；退款、提现与争议共享并发控制。退款有理由、权限、审批、尝试、终态与补偿，未知结果不释放预占。按成交快照分摊服务商、平台和税额；已结算/已提现退款进入明确资金处置流程。
- 验收：重复退款、两个并发部分退款都不超可退额；退款与提现同时发生不会双重支出；退款超时不再次盲目发起；退款确认后账务、可提现余额、订单展示一致；取消后晚到款被记录并处理而非丢弃。
- 入口：`lib/tap.ts` 的 `createRefund`、订单 Actions、消息订单卡片、后台订单；建议新增退款请求与处理器。

实现记录（2026-09-09）：已新增用户 `/refunds`、后台 `/admin/refunds`、`refund_requests`、`refund_attempts` 和持久化 `refund_events`。需求方可对已付款订单申请全额/部分退款并在审核前撤回；可退款订单、本人退款历史和管理员队列均使用时间/ID 复合游标分页，管理员可按活动或具体状态筛选。每笔订单的活动退款、已退款及剩余可申请金额由数据库全量聚合返回，页面不再依赖已加载的部分历史推算额度。订单锁串行计算“成功与活动退款”累计额，重复 client request ID 幂等，服务商/他人不能代申请。完成订单按成交 provider/platform 快照比例预留服务商金额，撤回、拒绝和明确失败释放，`PENDING/ACCEPTED/UNKNOWN/TIMED_OUT` 保持结果未知及冻结，只有 `REFUNDED` 更新累计退款和账本；开放退款阻止交付验收与双方注销。管理员审批写审计，外部执行由独立 `TAP_REFUNDS_ENABLED` 门控制，创建前持久化 attempt，返回与签名 Webhook 共用事件处理器。数据库已验证撤回、拒绝、未知后成功、累计超额、账本、权限、通知和导出；Supabase 网关已实际验证 completed 订单的 20 SAR 部分退款申请、管理员批准、测试 `REFUNDED` 事件、provider 可用余额从 85 降至 68，以及第二笔 10 SAR 退款撤回后冻结完全释放；参与方可读、第三方不可读。浏览器流程进一步验证已付款订单的 20 SAR 部分退款申请、管理员审批原因、用户端 `approved` 回显、受控 `REFUNDED` 事件后 `succeeded` 回显和订单 `refunded_amount`/`partial` 状态。Tap sandbox、已批准提现与退款资金短缺处置、税额分摊和真实到账仍待 D-01/D-03/D-02 验收，保持 In Review。

### OPS-02 实现争议、人工售后与超时处理

优先级：P1｜依赖：ORD-04、OPS-01、PAY-04、D-03、D-05｜状态：In Review

- 实施：双方提交争议、证据与诉求；管理员受理、补充材料、裁决、关闭；按规则冻结相关可结算资金，裁决连接继续履约/退款/结算；逾期和未处理申请进入队列。
- 验收：争议全过程可追踪，双方权限隔离；证据私密；冻结资金不能绕过提现；重复裁决不重复退款或释放；无授权的自动超时任务不能直接放款。
- 入口：`app/messages/page.tsx`、`app/admin/orders/page.tsx`；建议新增争议页面、记录和任务入口。

实现记录（2026-09-08）：已新增双方 `/disputes` 与后台 `/admin/disputes`；可争议订单、参与方完整历史、管理员状态队列和私有证据均使用时间/ID 复合游标分页，管理员默认聚焦活动争议并保留全局活动数量。活动争议每订单唯一并阻止交付、验收、另建退款及双方注销。完成订单会按扣除成功退款后的剩余服务商份额写 `dispute_hold`；管理员可标记审查中、释放资金继续履约、无处置关闭，或在同一事务将裁决转换为已批准的全额/部分退款并仅保留相应冻结。裁决包含原因、操作者和前后状态审计，双方及管理员收到幂等站内通知。新增私有 `dispute-evidence` bucket，仅允许双方/管理员在争议路径上传或读取 JPEG/PNG/WebP/PDF，已登记证据不能由浏览器直接删除；页面使用短期签名 URL。数据库已验证跨用户拒绝、退款/注销互斥、冻结释放、退款裁决、证据表与 Storage 隔离、通知和导出。证据清理后台、申诉/补充材料规则、逾期任务、真实多连接并发和 D-03 最终裁决规则仍待验收，保持 In Review。

### OPS-03 完善管理员写入与操作审计

优先级：P0｜依赖：BASE-03、BASE-04、CAT-02、PAY-04｜状态：In Review

- 实施：敏感后台动作集中到受控服务端入口，记录变更前后值、原因和操作者；检查实际写入结果；后台区分无权限、读取失败与空列表。管理员授撤权、账户停用、认证撤销有规则，避免失去全部管理入口。
- 验收：非管理员直调全部被拒绝；失败不改变 UI；重复提交和并发更新可预测；按订单 ID/交易 ID/用户检索；审计只读且不会包含密钥或无必要的私人内容。
- 入口：`app/admin/layout.tsx`、`app/admin/page.tsx`、`app/admin/orders/page.tsx`、`app/admin/providers/page.tsx`、`app/admin/services/page.tsx`。

实现记录（2026-09-09）：管理员授撤权、服务商认证、服务审核、AI 知识发布/回滚、提现审批、支付事件重试和账户停用/恢复均通过受信服务端入口写入；账户状态变更必须填写原因、禁止停用自己和仍有管理员权限的账户。停用状态已接入服务端身份检查、Proxy、公开资料、目录和既有 RLS，旧会话后续请求不能继续操作。新增只读审计页，可按动作、操作者邮箱或目标 ID 分页检索前后值；订单页可按订单 ID、Charge/交易号和用户搜索，并已改为不受旧 200 条上限影响的服务端分页。数据库已验证非管理员直调、客户端自停用、停用后的目录/消息访问和恢复；Supabase Local 浏览器流程已验证管理员审批服务、通过状态筛选、公开目录可见、`set_service_active` 审计检索，以及普通用户进入后台后被重定向。真实 Supabase 会话/Realtime 失效及页面级双语交互仍待 REL-02 验收，保持 In Review。

### OPS-04 统一仪表盘、订单和账务统计

优先级：P1｜依赖：ORD-05、PAY-05、OPS-01、OPS-03｜状态：In Review

- 实施：统一待交付、待验收、已完成、退款/争议、可提现统计口径；退款不计入待收入；收入与流水来自服务端聚合；后台订单不再固定只取最新 200 条；提供筛选、分页和必要明细导出。
- 验收：用包含部分退款、提现中、拒绝、失败与已提现的夹具手工核对总额；筛选结果与汇总口径一致；历史数据可查；接口失败不显示零收入。
- 入口：`app/dashboard/page.tsx`、`components/orders-table.tsx`、`app/admin/page.tsx`、`app/admin/orders/page.tsx`、`app/admin/withdrawals/page.tsx`。

实现记录（2026-09-09）：新增受信运营报表迁移与服务端 Actions，服务商仪表盘由同一快照返回分页订单、待交付/待验收/已完成、扣除开放退款后的待收入、开放退款/争议及账本可用/预留/已支付余额，并提供逐笔账本分页；读取失败保留不可用状态，不把收入伪装为零。后台总览统一聚合待审核服务、认证、提现、退款、争议和支付异常；服务审核、支付异常、订单及用户改为 50 条服务端分页，可按订单、服务、客户、Charge/交易号和状态筛选；服务商列表改为受信服务端双语搜索、认证状态过滤和创建时间/ID 复合游标分页；管理员提现按状态过滤并以请求时间/ID 分页，服务商自己的提现历史采用同一稳定边界，不再一次性下载整表或固定截断 100 条。订单可导出最多 5,000 条匹配结果的 JSON，超限显式失败。PGlite 已用 12 笔订单、20 SAR 部分退款、失败退款、争议冻结、拒绝提现及 50 SAR 已支付提现对照总额，并验证浏览器角色不能直调报表；182/182 单元/API/UI、57 份迁移、构建与 37/37 E2E 通过。真实旧库数据对账、真实 Supabase 页面交互、超过 5,000 条的异步报表及阿英/窄屏手工渲染仍待 REL-02/03 验收，保持 In Review。

### DATA-01 完成可靠且范围明确的数据导出

优先级：P1｜依赖：ACC-03、ORD-05、PAY-05、BASE-03｜状态：In Review

- 实施：列明用户资料/metadata、相关订单、会话和有权访问的收发消息、服务、评价、收藏、提现等导出范围；M5 增加 AI 会话、工单、通知、订阅和账单后同步扩展导出清单；完整分页，大数据异步处理；任一子查询失败不返回伪完整文件。第三方私人字段按既定范围过滤。
- 验收：资料中的地区/简介可导出；超过默认查询上限不截断；查询失败返回失败或显式部分结果清单；导出仅本人可获取，下载链接有权限/有效期；JSON 保留 UTF-8 和金额语义。
- 入口：`app/actions/user-data.ts`、`app/profile/page.tsx`；建议新增导出任务/结果状态。

实现记录（2026-09-09）：导出已改为受信数据库快照，数据库内聚合避免 PostgREST 默认行数截断，任何子查询失败会使整个 Action 失败；Auth metadata、资料、服务商与服务、服务商认证申请及材料元数据、双方订单/历史及成交快照、完整会话收发消息、评价、收藏、提现、交付及私有交付文件元数据、支付尝试/事件、账本、相关后台审计、删除申请、支持工单/回复、站内通知、AI 会话/turn、退款申请/尝试/事件、争议/证据索引、出款追踪/审计以及服务图片清理任务均纳入数据库快照版本 10。JSON 下载兼容入口使用导出封装版本 11，并按当前用户范围生成 15 分钟私有文件链接；任一签名失败时整次导出失败。资料页主入口改为鉴权的 `GET /api/user-data-export`，以流式 `tar.gz` 输出 `export.json` 和交付文件、争议证据、认证材料本体，不把整个归档一次性装入内存；文件下载中断会终止流，避免生成伪完整包。数据库已验证本人范围、空集合、超过首屏的 12 笔订单及快照、收发消息、服务商侧对象、认证材料索引、工单/通知、AI 会话、退款、争议、出款和私有交付文件索引，Action/API 覆盖数据库失败、身份失败、签名失败、归档内容及流中断；真实 Supabase 网关导出已确认需求方快照包含订单、交付版本、评价、退款事件、争议和证据，服务商快照包含同一订单及认证申请/文件索引。浏览器下载已验证文件名、gzip/tar 结构、版本 11 envelope、版本 10 本人资料快照及另一账户邮箱不泄露。超大账户异步生成、可恢复后台任务及未来订阅对象仍待实现，保持 In Review。

### DATA-02 完成注销申请、处理与恢复

优先级：P1｜依赖：DATA-01、OPS-01、OPS-02、PAY-05、D-05｜状态：In Progress

- 实施：将当前申请标记扩展为可处理队列，检查未结订单、未决退款、争议和余额；定义等待、撤回及再次登录行为；逐步处理 Auth、公开资料、服务、私有文件、会话与需保留交易记录。M5 接入后取消/终止后续订阅收费、处理未结账单，并落实 AI/工单/通知记录的保留与删除。外部 Auth/Storage/订阅操作采用可重试步骤。
- 验收：资格查询失败不能绕过阻塞；未处理余额有明确入口；处理完成后按规则不能登录/公开访问被删除资料，已登录标签页、旧 token、Realtime 和直接 API 也不能继续读取或新增私人业务数据；后续订阅收费按注销规则终止；失败后从未完成步骤续跑，不破坏依法/业务需保留的账务；后台和用户能看到状态。
- 入口：`app/actions/delete-account.ts`、`app/profile/page.tsx`、Auth/Storage 后台操作；建议新增注销请求管理。

进展记录（2026-09-09）：已用 `account_deletion_requests` 替换点击后立即匿名化的旧入口；申请会原子检查活动订单、开放提现及账本可用/预留余额，重复提交复用同一请求。用户保留登录和数据访问，可在进入 processing 前撤回；资料页区分状态加载失败、待处理和已开始处理；管理员只读队列支持开放/具体状态/全历史筛选，按最早请求时间和 ID 稳定分页，并分别显示 requested、processing、failed 全量计数，仍明确不提供未获规则授权的执行按钮。旧 `deletion_requested_at` 记录迁移为需人工核对的 processing 项；真实 Supabase 网关已验证无订单/余额账户的申请、本人 RLS 可见、他人不可见和 processing 前撤回。浏览器流程进一步验证有活动订单时显示阻塞错误、合格用户申请后能在管理员队列查看、执行保持禁用并说明原因、用户撤回后队列移除且数据库保留 cancelled 记录。Auth 用户/Storage 文件处理、保留规则、失败分步重跑、订阅终止和最终会话失效仍依赖 D-05 及后续对象，保持 In Progress。

### AI-01 加固并统一现有客服接口

优先级：P1｜依赖：BASE-04、D-06｜状态：In Review

- 实施：明确游客问答与登录功能边界；校验消息角色、类型、单条/总长度和轮数；跨实例生效的限流/配额、请求超时和取消；规范 DeepSeek/OpenAI 配置与失败返回。客户端提供重试，不将错误文本当正常对话永久发送给模型。
- 验收：非法 payload 返回可理解的 4xx；超限受控；无密钥/上游故障/超时可恢复；日志不记录密钥与不必要的完整对话；单纯调用 `getUser()` 不被当成已完成鉴权。
- 入口：`app/api/chat/route.ts`、`components/global-customer-service.tsx`、`components/ai-chat.tsx`、`.env.example`。

实现记录（2026-09-08）：`/api/chat` 仅接受 1～20 条 user/assistant 文本，限制单条、总字符、parts 和 64 KiB 请求体，禁止客户端 system 消息并要求最后一条来自用户。游客/登录用户分别通过数据库共享限流，访客网络标识只保存哈希；DeepSeek/OpenAI 配置有范围校验，上游 20 秒超时，429/502/503/504 错误稳定且日志不含对话、密钥或供应商正文。两个客户端均保留失败请求供重试，错误显示不写入模型历史；system prompt 已移除托管、支付方式、全员认证和人工支持等未证实承诺。真实模型质量、跨代理 IP 可信配置和沙箱超时仍待 REL-02 验收，保持 In Review。

### AI-02 完成知识、会话与本人业务查询

优先级：P1｜依赖：AI-01、BASE-03、ORD-01、D-06｜状态：In Review

- 实施：版本化平台 FAQ 和实际支付/售后规则；受控读取本人订单状态，业务数据来自服务端查询而非模型猜测；持久化授权会话和上下文裁剪；客服回答不得自行改订单、授权益、退款或放款。
- 验收：刷新后按保留规则恢复会话；另一用户订单不可读；提示注入不能越过查询权限；未知问题明确说明并提供人工入口；阿英两种语言使用一致业务规则；不再宣称未实现的托管/认证能力。
- 入口：`app/api/chat/route.ts`、客服组件；建议新增知识配置、会话表和受控业务查询层。

实现记录（2026-09-08）：已新增带 key/version/唯一活动版本约束的双语知识条目，内容覆盖市场范围、认证、订单交付、支付可用性和支持边界。游客保持无持久化；登录用户使用按本人隔离的 session 和幂等 turn，失败可重试、完成的重复 turn 直接复用答案，模型上下文只从数据库最近 10 个完成 turn 重建，刷新可恢复最多 50 个。问题中出现订单 UUID 时，服务端仅在当前用户是需求方或服务商时注入最小订单状态；客户端伪造历史和他人订单都不能成为授权依据。AI 会话与 turn 已加入数据导出。新增 `/admin/knowledge`：管理员可搜索并分页查看全部知识版本，以稳定请求 ID 发布双语新版本、带原因启用旧版本或停用当前版本；数据库串行分配版本号、保证每个 key 最多一个活动版本，并记录发布人、时间、变更原因及管理员审计，浏览器和非管理员不能直接写知识表。真实模型下的阿英文案质量、提示注入评测和最终 D-06 保留期限仍待验收，保持 In Review。

### AI-03 接通人工客服与必要业务通知

优先级：P1｜依赖：AI-01、OPS-02、BASE-03、D-05、D-07｜状态：In Review

- 实施：提供创建工单、关联订单、用户跟进、客服回复、分派与关闭；AI 升级人工需用户明确操作；为消息、交付、退款、提现和工单提供站内通知与已读状态。邮件等外部通知在确定渠道、模板和发送权限后接入。
- 验收：用户提交后有编号和状态，后台可以回复并被用户收到；双方之外不能读取工单；重复事件不重复通知；发送失败可重试；真实人员与服务时间未落实前不宣称 24/7 人工支持。
- 入口：客服组件、`app/messages/page.tsx`、`components/header.tsx`；建议新增工单/通知表、用户入口和后台队列。

实现记录（2026-09-09）：已新增用户 `/support` 与管理员 `/admin/support` 队列；用户主动创建工单，可选关联本人订单，双方回复使用 client request ID 幂等，管理员状态变更必须填写原因并写审计，关闭后禁止继续回复。用户/管理员工单及回复均使用更新时间/创建时间加 ID 的 50 条复合游标分页，跨页去重且读取失败保留现有结果。AI 助手提供显式“创建支持工单”入口且不宣称人工响应时间。消息、订单状态/交付修改、提现和工单回复会生成按事件键去重的站内通知；Header 显示数据库全量未读数，Header 与 `/notifications` 订阅当前用户 Realtime 变化，通知页以 30 条复合游标加载完整历史并支持逐条及全部已读。通知页会在 Realtime 订阅确认后补拉，避免首次读取与订阅之间发生事件时永久漏报。数据库已验证订单归属、非参与者隔离、重复创建/回复、管理员回复/关闭、通知/工单分页、未读总数、通知归属和导出覆盖；Supabase Local 双浏览器已验证用户建单、管理员回复、用户实时收到并打开通知、用户跟进、管理员关闭和关闭通知回显。真实人员排班、队列分派规则及获准后的外部邮件渠道仍待确认，保持 In Review。

### PLAN-01 定义并实现套餐目录

优先级：P2｜依赖：D-01、D-04、BASE-02｜状态：Backlog

- 实施：将静态三档套餐转为版本化配置，确定价格、计费周期、服务数量、佣金、搜索展示、支持等级与分析权益；解释税额、取消及超额服务规则，记录权益生效时点。
- 验收：界面、服务端与订单使用同一规则；历史套餐可追溯；免费计划也有明确权益；未经确定的功能承诺不作为可售权益。
- 入口：`app/services/provider/page.tsx`、`lib/tap.ts` 的固定佣金；建议新增套餐目录与规则计算模块。

### PLAN-02 实现套餐订阅生命周期

优先级：P2｜依赖：PLAN-01、PAY-02、PAY-03、OPS-01、D-02｜状态：Backlog

- 实施：接通选择、购买、续费、取消、到期、付款失败恢复及升级/降级；外部支付事件驱动生效。自动续费需确认支付能力和授权；若采用手动续期，页面和规则明确表达，不伪装成自动订阅。
- 验收：选择按钮产生可追踪订阅；未付款不授权益；重复回调只生效一次；取消到期正确降级；续费失败通知并按规则处理；账单、退款和历史查询可用。
- 入口：套餐页、支付适配层；建议新增 `subscriptions`、订阅事件、管理页面。

### PLAN-03 落实所有保留的套餐权益

优先级：P2｜依赖：PLAN-02、CAT-01、CAT-03、AI-03、OPS-04｜状态：Backlog

- 实施：服务数量限制在服务端并发校验；佣金按成交快照应用；真实显示 premium/VIP 标识与约定搜索排序；支持队列体现等级；高级分析使用真实业务聚合。降级超额不直接删除服务或影响进行中订单。
- 验收：免费/专业/高级在数量上限边界、并发发布、佣金、展示和支持路径均有用例；客户端篡改计划无效；到期及时撤销后续权益；旧订单佣金不被降级改写；每项保留宣传都有可执行证据。
- 入口：`app/my-services/page.tsx`、`app/services/seeker/page.tsx`、`app/actions/orders.ts`、后台支持与仪表盘。

### UX-01 完成双语、移动端与可访问性验收

优先级：P1｜依赖：BASE-04；各业务任务完成后复验｜状态：In Review

- 实施：统一日期/金额/数字和阿英文案；检查 RTL 逻辑布局、长文本、表格、对话框、错误/加载/空态；补正确语义标题、键盘与焦点管理。核实主题入口：已有主题能力若保留则接线并检查颜色，否则不把暗色 class 当作完成暗色模式。
- 验收：桌面与手机完成注册、搜索、聊天、下单、交付、提现和后台关键操作；阿英切换不丢草稿；键盘可完成弹窗，关闭后焦点返回；屏幕阅读标签明确；主题若提供则可切换、持久化且正文/错误均可读。
- 入口：`components/header.tsx`、`components/language-provider.tsx`、`components/theme-provider.tsx`、`app/layout.tsx`、各业务页面。

实现记录（2026-09-09）：根布局已接入 `next-themes`，普通 Header 与后台均提供有名称的亮/暗主题按钮，跟随系统为默认值并持久化用户选择；语言切换同步并持久化根节点 `lang`/`dir`。订单、报价、消息和仪表盘的 SAR 金额按 `ar-SA`/`en-SA` 格式化，搜索、图标间距、浮动入口和徽标改用逻辑方向；图库导航、筛选、图片/技能增删、后台菜单与翻页等图标按钮补充可访问名称，隐藏的图片删除按钮可由键盘聚焦显示。Playwright 已验证主题与语言刷新保持、390px 下首页/About/套餐/登录无横向溢出，以及公开可见按钮均有文本、ARIA、title 或关联 label；37/37 E2E 通过。真实登录后的聊天、下单、交付、提现、后台窄屏/键盘/焦点回归，以及全部长阿文和高对比主题人工检查仍待 REL-02，保持 In Review。

### UX-02 清理静态演示与不实业务承诺

优先级：P1｜依赖：CAT-04、D-04、D-05、D-07｜状态：In Review

- 实施：核对首页固定价格、About 统计、商业/税务登记占位、支持邮箱与服务时间、认证/托管/争议承诺；统计接真实聚合或采用不含虚构数字的文案。核对条款、隐私与实际数据处理及退款流程，英语内容与阿语覆盖范围一致。
- 验收：没有 `XXXXXXXXXX` 等生产占位、虚构客户数/好评率、无动作按钮；每个宣传能力映射到已验收任务或明确产品决策；独立需求发布若保留，已另行补齐完整任务并实施。
- 入口：`app/page.tsx`、`app/about/page.tsx`、`components/footer.tsx`、`app/terms/page.tsx`、`app/privacy/page.tsx`、套餐页。

实现记录（2026-09-08）：首页固定类目价格、虚构用户/项目/满意度统计、全员认证、资金保护、保证收入/法律保护、24/7 人工支持、独立需求发布、商业/税务登记占位和未确认支持邮箱已删除或改为当前功能描述。未上线的 99/199 SAR 套餐与无动作购买按钮已替换为“尚未开放”说明；条款不再预先断言 VAT、退款和支付方式，隐私页指向已实现的数据导出/删除申请。E2E 检查公开页面不存在这些占位、虚构数字和套餐按钮。最终法律文本、正式实体/联系渠道以及 D-04/05/07 决策仍待业务确认，保持 In Review。

### REL-01 更新运行、配置与维护文档

优先级：P1｜依赖：BASE-02、BASE-05、AI-01；随业务实现持续更新｜状态：In Review

- 实施：补项目 README、环境变量与启动方式、数据库安装/升级、测试账号、支付回调配置、后台任务执行方式和失败处理；修正 AI README 指向不存在路径的问题。明确 `ai_services/` 训练资产用途与是否部署；CI 清理遗留 Stripe 配置，配置与当前 Tap 路线一致。
- 验收：新环境按文档可启动并运行测试；全部引用文件/路由存在；区分必填、可选、服务端与公开变量；仓库和浏览器包不包含密钥；依赖升级仅在实施需要时单独说明。
- 入口：`AI_CUSTOMER_SERVICE_README.md`、`.env.example`、`.github/workflows/ci.yml`、`package.json`；建议新增 `README.md` 与运维说明。

实现记录（2026-09-09）：根 README 已记录 npm/Node 基线、本地启动、环境变量与资金开关边界、数据库 fresh install/旧库采用、完整验证命令、主题/双语能力、主要路由和外部发布前提；Supabase README 已覆盖五个 bucket、私有交付/认证文件和迁移警戒。AI 指南对应真实 `/api/chat`、限流、超时、DeepSeek/OpenAI 配置及 `ai_services/` 未部署实验资产。新增 [运维与发布运行手册](docs/operations-runbook.md)，列明环境与角色、测试账号保管、Tap 回调、单笔/批量 Charge 对账、Refund/Payout 未知结果处置、业务队列、监控最小项、备份恢复、向前修复、代码回退和发布证据；BASE-01 文档已把旧远端快照与当前本地 57 份迁移契约分开，移除“CI 仍注入 Stripe”等过期本地结论。CI、`.env.example`、README 与当前 Tap 路线一致，全部文档链接通过。真实全新预发布环境复现、监控接收者、备份恢复演练和正式值班责任人仍待 REL-03 验收，保持 In Review。

### REL-02 完成全链路与故障回归

优先级：P0｜依赖：全部业务任务；BASE-05 提供设施｜状态：In Progress

- 实施：落实第 6 节验收矩阵，补真实本地数据库权限/事务测试、业务集成测试和双角色浏览器 E2E；补验 M2 留下的真实支付、退款和账务关联场景；M5 后复验包含全部新数据及订阅终止的 DATA-01/02；UI 和文档变更都做渲染检查；修复所有影响主链路的失败并保留证据。
- 验收：阿英、需求方/服务商/管理员完整流程通过；金额、状态、权限和故障恢复场景无未解释失败；不能只用直接写库或全量 mock 替代用户操作验收。
- 入口：`tests/`、`e2e/`、CI；建议新增验收证据索引，关联任务编号与对应 commit。

进展记录（2026-09-09）：已新增 PostgreSQL 17 的 `supabase/config.toml` 与 `supabase:start/stop`、`test:supabase`、`test:e2e:supabase` 命令。启动脚本用临时空 Docker credential 配置绕过 `docker-credential-desktop` 卡死，不修改用户配置且不输出本地 key；已完成首次初始化、重复 stop/start 和多次 `db reset`，每次均执行 57 份迁移；`npm run lint:db` 以 warning 为失败条件，修复无用锁变量和遗漏的管理员 note 审计后达到零警告。API 网关脚本通过真实 Auth/Mailpit/PKCE、PostgREST、Storage 和 Realtime 验证一次性注册/恢复邮件、新旧密码、角色/RLS、受限整表/RPC、服务审核目录、公开图片归属和消息隔离；进一步以测试到账事件完成固定价订单、私有交付文件上传/双方读取/第三方拒绝、修改与再次交付、验收、评价、认证材料审批、争议证据、20 SAR 部分退款终态、退款撤回、通知隔离、双方数据导出及删除申请/撤回，并验证三类私有引用文件不能被浏览器删除。Realtime 冷启动改为在 30 秒窗口内重发测试消息，收到首个订阅事件即停止；应用仍保留订阅后补拉。10/10 Playwright 集成场景通过 seeker 认证生命周期、provider 三步入驻、管理员服务审核、需求方/服务商双上下文订单、真实图片服务生命周期、私有材料认证、本人数据归档下载、注销申请/撤回、部分退款结算及管理员支持闭环；同时验证两版私有交付、签名读取、Realtime 状态推进和通知补拉，并修复 callback origin 跨 host 丢失 PKCE cookie、Supabase Local 图片 URL 校验及通知订阅竞态。普通 E2E 结果为 37 passed、10 integration skipped；集成命令未加入默认 CI。真实 Tap/AI、退款/出款的真实外部结果、异常网络恢复、更多多标签并发及全部业务矩阵仍待扩展，保持 In Progress。

### REL-03 完成沙箱、运行恢复和发布准备

优先级：P0｜依赖：REL-02、D-01～07 已决策且范围内追加任务完成｜状态：Backlog

- 实施：在获准的独立环境验证 Auth 邮件、Tap 回调/退款/提现/套餐、模型和人工通知；验证任务重启、备份恢复、失败事件处理和监控告警。准备迁移、发布、应用回退及数据库向前修复方案，明确负责人和运营前提。
- 验收：支付成功但回调丢失、任务崩溃、数据库短暂不可用、重复事件可恢复；恢复演练核对订单与账务一致；受信凭证不进入客户端；业务负责人确认税额规则、收付能力、条款内容和人工支持安排。完成准备不等于已获准生产发布。
- 入口：部署配置、后台任务、数据库迁移、日志与运行手册；不得用真实用户交易作为未授权测试数据。

## 6. 必须保留的验收矩阵

每个测试记录环境、数据夹具、执行者、结果、截图/trace/日志引用。涉及金额与权限的场景必须有数据库状态断言，只有页面 Toast 不足以证明成功。

| 场景组 | 最少覆盖场景 | 主要任务 |
| --- | --- | --- |
| 数据安装/升级 | 空库、旧金额字段、混合字段、已有用户、重复 provider、迁移失败与恢复 | BASE-01～03 |
| 身份隔离 | 游客、需求方 A/B、服务商 A/B、管理员、后台任务；浏览器与直接 API 两层 | BASE-03、ACC-01～03 |
| 反馈故障 | 断网、403、数据库错误、零行更新、空列表、成功后刷新 | BASE-04 |
| 服务生命周期 | 草稿、图片失败、审核拒绝、重新提交、停用直链、删除关联订单 | CAT-01～04 |
| 收藏 | 添加、重复点击、取消失败、重登、下架目标、跨用户拒绝 | CAT-05 |
| 消息 | 文本/卡片失败重试、重复事件、离线补拉、双向隐藏恢复、清空后发卡片、历史分页、多标签页未读 | MSG-01～03 |
| 报价/建单 | 固定价、工时、起步价、过期报价、恶意金额、自购、关联服务不匹配、双击并发 | ORD-01～03 |
| 付款 | 新建、复用、过期、外部成功本地失败、用户关闭页、重复/乱序回调、金额不符、旧 Charge 晚到账 | PAY-02～03 |
| 履约/评价 | 交付、修改、再次交付、验收重试、历史写失败、未验收评价拒绝、11+ 条评价全量统计 | ORD-04～05 |
| 提现 | 同余额并发申请、拒绝释放、重复审批、外部结果未知、成功后进程崩溃、晚回调、账务核对 | PAY-04～05 |
| 售后 | 取消与到账竞争、全额/并发部分退款、重复退款、退款与提现并发、已提现退款、争议冻结与裁决 | OPS-01～02 |
| 后台 | 非管理员直调、写失败、权限变化、审计、超 200 笔订单分页、金额聚合 | OPS-03～04 |
| 数据权利 | 导出分页/子查询失败/他人数据过滤、M5 新数据覆盖、注销阻塞/撤回/失败重跑/旧会话失效/Auth 与文件处理/终止订阅收费 | DATA-01～02 |
| 客服 | 阿英 FAQ、非法输入、超时、限流、上下文恢复、越权订单请求、人工接管与回复 | AI-01～03 |
| 套餐 | 初购、续费、升级/降级、周期末取消、失败恢复、重复事件、并发服务数量上限、佣金快照、全部保留权益 | PLAN-01～03 |
| 体验与发布 | 阿英/RTL、手机/桌面、键盘/焦点、主题（若提供）、文案真实性、沙箱真实回调、备份恢复 | UX-01～02、REL-01～03 |

基础命令沿用仓库现有脚本，后续新增数据库测试按 BASE-05 补入文档与 CI：

```bash
npm ci
npm run lint
npx tsc --noEmit --incremental false
npm test
npm run build
npm run test:e2e
```

运行前按 `.env.example` 配置隔离测试环境；占位配置只能证明构建/匿名页面，不用于认定登录、数据库、支付或 AI 联调通过。缺少外部账户时相关任务保留待验收状态，并记录具体阻塞条件。

## 7. 开工顺序与完成记录

建议第一批按顺序执行 BASE-01、BASE-02、BASE-03，同时独立推进 BASE-04、BASE-05。之后完成 ACC-01/02，再做 CAT-01/02 与 ORD-01/02。支付能力申请和 D-01～07 的业务决策可以同步进行，不应等到支付代码写完才发现外部能力不可用。

每个任务完成时在本文件中更新状态，并附以下记录；大任务可以追加子编号，但不要删除未完成项或以隐藏入口代替最终补全。

```text
任务：例如 PAY-03
状态：Backlog / In Progress / Blocked / In Review / Done
负责人：
实施日期与代码版本：
实际修改文件：
关联业务决策与迁移：
验证环境与执行命令：
通过的正常/权限/失败/重试场景：
截图、trace、日志或账务对照证据：
未完成事项及明确原因：
迁移恢复/回退注意事项：
```

统一 Done 条件：

- 用户入口、服务端规则、数据库持久化/权限、错误恢复全部接通。
- 具体验收场景通过，成功后刷新或重新登录仍保持正确结果。
- 金额操作满足幂等、并发和对账不变量；不能把结果未知标成成功或失败。
- 涉及 UI 的改动完成阿英与目标屏幕尺寸渲染检查；涉及表结构的变更有迁移与旧数据验证。
- 相应自动化回归、配置文档和运行说明更新；没有把外部环境未验证项计为完成。

## 8. 审查证据入口

以下链接指向当前仓库实现，用于开工前复核；实现后应更新结论，不能持续引用旧缺陷作为现状。

| 发现 | 代码入口 |
| --- | --- |
| Toast 缺少挂载 | [根布局](app/layout.tsx)、[Toast 状态](hooks/use-toast.ts) |
| 登录/找回密码与角色分歧 | [登录](app/auth/login/page.tsx)、[注册](app/auth/signup/page.tsx)、[资料](app/profile/page.tsx)、[权限](proxy.ts) |
| 创建服务即 active、图片部分失败 | [我的服务](app/my-services/page.tsx) |
| 收藏缺添加 | [收藏列表](app/favorites/page.tsx) |
| 消息丢草稿、隐藏/清空恢复 | [消息页](app/messages/page.tsx) |
| 报价含税显示与订单金额不一致 | [报价对话框](components/create-order-dialog.tsx)、[订单 Actions](app/actions/orders.ts)、[金额及支付封装](lib/tap.ts) |
| 回调签名与普通会话客户端 | [Tap Webhook](app/api/webhooks/tap/route.ts)、[服务端 Supabase](lib/supabase/server.ts) |
| 入驻占位、提现断链 | [Tap Connect Actions](app/actions/tap-connect.ts)、[后台提现](app/admin/withdrawals/page.tsx) |
| 历史过滤与评价资格 | [历史页](app/history/page.tsx) |
| 导出与注销未闭环 | [导出](app/actions/user-data.ts)、[注销](app/actions/delete-account.ts) |
| AI 实现与文档不一致 | [实际 API](app/api/chat/route.ts)、[旧 AI 文档](AI_CUSTOMER_SERVICE_README.md) |
| 套餐仅静态展示 | [服务商套餐页](app/services/provider/page.tsx) |
| 缺少数据库完整基线 | [现有增量迁移](supabase/migrations/20260424000000_tap_payment_fields.sql) |
| 测试与 CI 边界 | [认证 E2E](e2e/auth.spec.ts)、[支付工具单测](tests/lib/tap.test.ts)、[CI](.github/workflows/ci.yml) |

支付协议参考：[Tap 官方 Webhook 文档](https://developers.tap.company/docs/webhook)、[退款状态与流程](https://developers.tap.company/reference/refunds)、[Marketplace 当前流程](https://developers.tap.company/docs/marketplace-overview)及[Destination 状态查询](https://developers.tap.company/reference/retrieve-a-destination)。2026-09-09 审查已对照字段拼接签名规则、全额/部分退款、`REFUNDED`/`ACCEPTED` 等状态语义、Marketplace 独立密钥、KSA KYC、destination 状态查询，以及自动或 dashboard 出款前提；实施剩余 PAY-01～05 和 PLAN-02 前仍须核对商户实际开通能力与合同配置。
